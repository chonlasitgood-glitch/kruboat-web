rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // 1. LIBRARY (เดิมคือ Projects - จัดการ Source Code)
    match /library/{document=**} { 
      allow read: if true; 
      allow write: if request.auth != null; 
    }

    // 2. PROJECTS (ของใหม่ - จัดการ Web Projects)
    match /projects/{document=**} { 
      allow read: if true; 
      allow write: if request.auth != null; 
    }

    // 3. APPS
    match /apps/{document=**} { 
      allow read: if true; 
      allow write: if request.auth != null; 
    }

    // 4. NEWS
    match /news/{document=**} { 
      allow read: if true; 
      allow write: if request.auth != null; 
    }

    // RoomMate & Tools
    match /rm_students/{document=**} { allow read, write: if request.auth != null; }
    match /rm_homeworks/{document=**} { allow read, write: if request.auth != null; }
    match /rm_games/{document=**} { allow read: if true; allow write: if false; }
    match /rm_config/{document=**} { allow read: if true; allow write: if request.auth != null; }
    match /rm_logs/{document=**} { allow create, read: if request.auth != null; }
    match /scoreboards/{document=**} { allow read, write: if true; }

    // Messages
    match /messages/{document=**} { 
      allow create: if true; 
      allow read, update, delete: if request.auth != null; 
    }

    // Config
    match /config/{document=**} { 
      allow read, write: if request.auth != null; 
    }

    match /events/{roomId}/{document=**} {
      allow read, write: if true; 
    }

    // Allow read/write for your specific collection path used in the app
    match /artifacts/{appId}/public/data/spatial_math_levels/{levelId} {
    allow read, write: if request.auth != null;
    }

    // Default rule to deny everything else (good practice)
    match /{document=**} {
    allow read, write: if false;
    }
  }
}